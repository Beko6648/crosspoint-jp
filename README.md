# CrossPoint Yomuka

> [!IMPORTANT]
> **Beko6648フォークの開発状況メモ**
>
> **ベースの上流バージョン**: 上流 **`v0.6.3`**（本ごとの読書設定・読書設定テスト表示・設定バックアップ等 + v0.6.1の安定化）。フォーク独自機能（インライン画像・ページ送り中インジケータ・章一覧のTOC除外・縦書き段落配置の固定・三点リーダー改行修正など）を維持しています。**変更の詳細・技術的経緯は [フォーク開発状況メモ(詳細)](docs/fork-notes.md) を参照してください。**
>
> **フォーク独自実装（上流との差分が残るもの）**
>
> **表示・縦書き**
> - **縦書きの段落配置を両端揃え(Justify)に固定**:縦書きでも行頭インデントが効くように
> - **行頭が全角空白で始まる段落の二重インデントを解消**:上流PR #21として投稿
> - **縦書き句読点描画の二重`loadVertData`呼び出しを解消**(2026-08-19):挙動不変のリファクタ
> - **ページ送り中インジケータ**:ステータスバーに送り方向の矢印を一瞬表示
> - **章一覧から「ファイル途中を指す目次項目」を除外**:Kindleと同様の章一覧に
> - **三点リーダー(…)の極長連続で改行が破綻する問題を修正**(2026-08-25):「…」が300個以上連続する箇所で、連続全体が1つの改行不能ワードになり画面端まで点が埋まる問題を修正。「…」を個別文字に分割し正しく折り返す。実機確認済み(2026-08-25)
>
> **インライン画像**
> - **インライン画像のサンセリフ(ゴシック)表示対応**(2026-08-19):サンセリフでもインライン画像が専用ページ化しないよう判定を修正。併せて段落末の画像が画面中央まで下がる問題も修正(画像で行が終わる末尾は元の位置を維持)。実機確認済み(2026-08-19)。※ インライン画像の表示サイズ・縦位置の微調整は明朝とサンセリフでフォントメトリクスが異なるため後回し(既知の問題として許容)
>
> **ファイル管理**
> - **ファイルの移動**:ロングプレスでファイル/フォルダを任意のフォルダへ移動できるように
> - **binファイルを本一覧に表示**:SDカード一覧からFWファイルを整理できるように
> - **ファイル転送モード待機中の電池消費を削減**:アイドル時にWiFi省電力+ポーリング削減、転送時は自動復帰
>
> **フォント・SD**
> - **拡張フォント「NotoSerifJp-Ext」とフォントビルドスクリプト拡張**(2026-08-18):CJK拡張A漢字(約30,007字)を収録した明朝フォント
> - **フォントのDL配布元をフォークの`sd-fonts`に変更**(2026-08-19):NotoSerifJpExt含む日本語5ファミリーを端末からダウンロード可能に
> - **SDフォントのkern行キャッシュ**:メモリ断片化時のkern確保失敗を抑制(実機確認済み)
>
> **安定性・ビルド**
> - **バッテリー満充電時のUSB Serial不活性を修正**(上流 zrn-ns PR#69 移植、2026-08-20):Serial 初期化の無条件化+250ms delay+`setTxTimeoutMs(1)` で修正
> - **drawArc/fillArc の走査ベース高速化**(上流crosspoint-reader #1540移植、2026-08-20):円弧描画を走査ベースに変更。実機確認済み
> - **フォーク専用 clang-format ツールチェーン＋CI**(変更行のみ整形・上流コードを汚さない)
>
> **上流に取り込まれて差分がなくなったもの**（フォークが先に実装したが、現在のベースで上流に取り込み済み）
> - 画像ページの描画速度改善(14秒→4秒) / インライン画像の文字化 / XTCHスリープ表紙のグレースケール化 / キャッシュ生成終了時のフォントキャッシュ解放
>
> **意図的に取り込んでいない上流機能**: 縦書きでの矢印の直立表示（Kindle版と表示が一致しないため）
>
> **未解決(既知の問題)**: XTCHの「メモリエラー」(ヒープ断片化・保留) / インライン画像の表示位置が明朝体以外で少しずれる(2026-08-19)
>
> **ブランチ運用**:`main`ブランチ=実機適用中の安定版FWの基準。改修時は`main`から新ブランチを派生し、成功したら`main`に取り込む。

> 日常の操作、キャッシュ、診断レポートは[基本操作と困ったときの確認](docs/basic-operations-ja.md)を、フォント導入は[日本語フォントの導入](docs/cjk-fonts.md)を参照してください。

CrossPoint Yomuka は、Xteink X3 / X4 向けの日本語読書に重点を置くオープンソースファームウェアです。本家 CrossPoint Reader と CrossPoint JP を基にした、非公式のコミュニティフォークです。

日本語EPUBの縦書き、ルビ、縦中横、SDカードの日本語フォント、青空文庫、EPUB/TXT/XTCの読書を扱いやすくすることを目指しています。

> [!WARNING]
> CrossPoint Yomuka は非公式ファームウェアです。端末メーカー、本家CrossPoint Reader、CrossPoint JPによる承認・サポート・保証はありません。導入・更新は自己責任で行い、事前にSDカード内の必要なデータをバックアップしてください。

## 対応端末

| 端末 | 状況 |
|---|---|
| Xteink X3 | 対応。UC8253 / UC8279の表示コントローラを判別し、X3固有の入力・スリープ・表示を確認対象とします。 |
| Xteink X4 | 対応。X3と同じファームウェアで動作します。 |

端末の個体差、SDカード、書籍の構造により動作や表示は変わる場合があります。

## 対応フォーマットと制限

| 形式 | 対応状況 |
|---|---|
| EPUB | 縦書き・横書き、ルビ、画像、書籍スタイルに対応 |
| TXT | 横書きに対応 |
| XTC / XTCH | 対応 |
| PDF | 非対応 |

EPUBのCSS・固定レイアウト・段組み・特殊な位置指定を完全に再現するものではありません。画像の多い書籍、複雑なCSS、特殊な画像形式では表示が簡略化されたり、初回解析に時間がかかったりする場合があります。

## v0.6.3 の主な内容

### 本ごとの読書設定と確認画面

- EPUBを開いている間に、読書メニュー → `読書設定` → `この本の設定` から、現在開いている本だけに読書設定を保存できます。全体設定を変えずに、書籍ごとに縦横書き、フォント、文字サイズ、行間・文字間隔、余白、ルビ、書籍スタイルを使い分けられます。
- 同じ画面の `全体設定を使用` を選ぶと、その本だけの設定を消して全体の読書設定へ戻せます。
- `読書設定テスト表示` では、縦書き／横書き、ルビ、長文の改行、句読点・括弧、英数字・縦中横、長音、太字を、現在の設定で確認できます。テスト表示中に `決定` を押すとルビ位置の調整に入り、画面の操作表示に従ってX/Y方向を調整します。
- ルビ位置は、全体の読書設定、本ごとの設定、設定のバックアップ／復元で扱えます。v0.6.2以前の設定はルビ位置 0 として安全に読み込みます。

### EPUB表示の改善

- ルビを含む縦書き列では、設定した行間を基本に必要な分だけ間隔を補い、本文との衝突を避けます。
- Noto系・ZenMaruGothicの縦書きで、全角コロン／セミコロンと半角長音の字形・位置を改善しました。
- SDフォント5系統を更新しました。記号 `✓ ✿ ✽ ❖ № ℡` の収録状況はフォントごとに異なります。

## v0.6.0 からの基盤機能

### X3 / X4 共通ファームと表示の安定化

- FreeInk SDKを使うX3/X4共通ファームへ移行
- X3のUC8279を含む表示コントローラ判別に対応
- 縦書き、ルビ、太字、SDフォント、画像、ページ送り、スリープ・復帰をX3/X4で確認する運用へ更新

### EPUB読書とキャッシュ

- 縦書き・横書き別のフォント、文字サイズ、行間、字間、余白、ルビ設定
- `CrossPoint優先`、`書籍優先`、`バランス`の書籍スタイル
- 読書キャッシュの進捗表示、中断・再開、書籍単位の削除と再生成
- CSSや長い本文、画像キャッシュに関するメモリ保護と検証の改善

### SDカード日本語フォント

- 日本語フォントをファームウェアへ埋め込まず、SDカードへ導入できる方式
- [SD Card Fonts Release](https://github.com/ponto1216-ai/crosspoint-jp/releases/tag/sd-fonts)から、フォントごとの`Yomuka-Font-<family>.zip`をSDカード直下へ展開
- 端末からのフォントダウンロード、途中中断、HTTP Rangeを使った再開、完了前の既存フォント保護

### 診断と安全な調査

- 設定の`本体` → `診断`で、ファームウェア版、端末、SD状態、heap、読書キャッシュ数、直近ログを確認
- 診断レポートをSDカードへ保存し、書籍本文や画像を共有せずに不具合報告へ添付可能

今後の優先順位は[ロードマップ](docs/roadmap.md)を参照してください。

## インストールと更新

1. [Releases](https://github.com/ponto1216-ai/crosspoint-jp/releases)から、使用する版の説明と配布物を確認します。
2. 更新前に、必要な書籍、フォント、設定、診断レポートをバックアップします。
3. リリースノートに記載された方法で更新します。SDカード更新を使う場合は、端末の`設定` → `本体` → `SDカードファームウェア更新`を開きます。
4. 更新中は電源を切ったり、SDカードを抜いたりしません。
5. 更新後は、少なくとも書籍を1冊開き、フォント、ページ送り、スリープ・復帰を確認します。

> [!CAUTION]
> 配布物、対応する更新手順、既知の制限はリリースごとに異なります。古い手順や別プロジェクト用のファイルを混ぜず、必ず導入する版のリリースノートを確認してください。

## フォント

日本語読書用フォントは、PCでSDカードへ展開する方法を推奨します。ZIPをSDカード直下へ展開すると、`/.fonts/<family>/`へ必要な`.cpfont`ファイルが配置されます。詳しい手順、Wi-Fiダウンロードとの違い、再開、フォントごとの文字範囲の注意は[日本語フォントの導入](docs/cjk-fonts.md)を参照してください。

## 不具合報告

不具合は[GitHub Issues](https://github.com/ponto1216-ai/crosspoint-jp/issues)で報告してください。次の情報があると調査を進めやすくなります。

- Yomukaのバージョン、端末（X3 / X4）、発生日時
- 問題が分かる写真と再現手順
- 書籍の形式と、問題のページが画像ページかどうか
- `設定` → `本体` → `診断`から保存したレポート

購入書籍など、著作権のあるEPUB本文・画像そのものはIssueへ添付しないでください。書籍名、画像形式・寸法・容量、問題ページの構造、診断レポートだけで調査できる場合があります。

## ライセンスと謝辞

ライセンスはリポジトリ内のライセンスファイルを確認してください。フォント、ライブラリ、画像には個別のライセンスが適用される場合があります。

CrossPoint Yomuka は、本家CrossPoint Reader、CrossPoint JP、関連するオープンソースプロジェクトの成果を基にしています。開発者、貢献者、ライブラリ作者、フォント作者の皆さまに感謝します。
